# Backend bridge placeholder

This folder is reserved for a future MySQL-backed backend layer.

Suggested services:
- REST API for admin operations
- MySQL connection and schema setup
- Admin dashboard endpoints for announcements and analytics
